__pycache__/
*.session
.env
docs/*.pdf
.DS_Store

# Herramientas locales de análisis de código (no forman parte del proyecto)
.serena/
.tokensave/

# Artefactos locales de wrangler
web/.wrangler/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Superficie SEO generada por scripts/build_seo.py en cada despliegue:
# vive solo en el árbol de trabajo (wrangler --commit-dirty), NUNCA en git.
web/municipio/
web/sitemap.xml

# Vista previa del resumen semanal (scripts/resumen_semanal.py --dry-run):
# artefacto local de validación, nunca en git.
web/data/.resumen_preview.html
web/data/.resumen_preview.txt
